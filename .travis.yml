language: rust
rust:
  - stable
  - beta
  - nightly
cache: cargo
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
  env: COMPILER_NAME=gcc CXX=g++-5 CC=gcc-5
  include:
    addons:
      apt:
        packages:
          - g++-5
